export enum TortuePipelineEvent {
  COMPONENTS_BUILT = "componentsBuilt",
  PAGES_BUILT = "pagesBuilt",
  LAYOUTS_BUILT = "layoutsBuilt",
  CONFIG_LOADED = "configLoaded",
  RENDER_FINISHED = "renderFinished",
}
